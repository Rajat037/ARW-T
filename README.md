
  # Website design with features

  This is a code bundle for Website design with features. The original project is available at https://www.figma.com/design/Npme9jlKZhmt05yktns5kl/Website-design-with-features.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  